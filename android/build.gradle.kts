// Top-level build file. Plugin versions are declared centrally in
// settings.gradle.kts (pluginManagement), so subprojects apply them without a
// version. Declaring them here with `apply false` keeps them on the root
// classpath for all modules.
plugins {
    id("com.android.application") apply false
    id("org.jetbrains.kotlin.android") apply false
}
